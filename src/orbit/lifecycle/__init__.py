"""Data-lifecycle facade package (`data-lifecycle-conventions`) — account
erasure lives in `lifecycle/erase.py`; a future retention/export mechanism
would extend this package rather than scattering deletion logic across
routes/repositories.
"""
