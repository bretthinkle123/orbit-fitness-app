---
feature: greenfield
criteria_total: 34
delegated_criteria: [AC33]
perf_budget: "p95 < 300ms @ ~10 concurrent (GET /fuel, GET /train, POST /fuel/entries)"
notes: >
  Derived from PROJECT.md / CLAUDE.md "What done means" + the requirements/design brief.
  Native iOS (SwiftUI) is a REDUCED-ASSURANCE target: iOS criteria are verified by
  XCTest/XCUITest/snapshot + human diff-review and are never claimed "gate-verified"; the
  backend carries full deterministic gate coverage. Each criterion still maps to a concrete
  test/mechanism so it rides criteria_covered. AC33 (ASVS L1/L2 reconciliation) is delegated
  to the security stage.
---

# Acceptance criteria — Orbit greenfield

| ID | Criterion | File / layer | How verified |
|----|-----------|--------------|--------------|
| AC1 | `GET /health` returns 200 with **no external dependencies** (no DB/Firebase); exempt from rate limiting | `src/orbit/routes/health.py` | integration test hits `/health` with DB/Firebase down → 200; smoke check |
| AC2 | Every domain endpoint requires a valid Firebase ID token; missing / invalid / expired → 401 (unauthenticated-access-denied) | `src/orbit/auth/` | per-route unauth test (no token + expired token → 401) |
| AC3 | Row-level ownership: a principal only reads/writes its own rows; another owner's id → 404 on read/update/delete; list returns only caller's rows (IDOR/BOLA) | `src/orbit/repositories/` | cross-owner denial test on every owned resource |
| AC4 | `POST /me/bootstrap` idempotently creates profile + 13 muscle base levels + defaults **atomically** (one transaction); **new users start EMPTY** of food/sets/weight | `src/orbit/routes/me.py`, `repositories/profile.py` | integration test: bootstrap twice → one profile, 13 levels, zero domain rows; **atomic multi-insert (T2-5 / ASVS 2.3.3): a fault-injected failure mid-insert rolls the whole transaction back — no partial profile/levels persist** |
| AC5 | In-app account deletion (`DELETE /me`, Settings action) **erases all user rows across every domain table + deletes the Firebase identity**, in a transaction, leaving an audit event (App Store 5.1.1(v) + GDPR erasure) | `src/orbit/lifecycle/erase.py`, iOS `SettingsSheet` | erasure-cascade test (each raw store empty, Firebase delete called, audit survives); **atomic-rollback test (T2-5 / ASVS 2.3.3): a forced failure after the N-th table delete rolls the whole cascade back — no table shows a partial delete, verified by an integration test**; XCUITest delete flow |
| AC6 | Seeded quick-food catalog with macros available via `GET /catalog/quick-foods` | `migrations/`, `routes/fuel.py` | integration test: catalog returns seeded rows + macros |
| AC7 | `POST /fuel/entries` creates a day-keyed food entry (from `quick_food_id` or explicit macros); `GET /fuel` returns entries grouped by meal + totals + targets + static coach message; bounded ≤200/day | `routes/fuel.py`, `repositories/fuel.py` | integration tests: create + grouped read + totals; bound enforced |
| AC8 | Macro/kcal targets canonical as **grams** (2,350 kcal · P185/C240/F72); split % **derived** ≈31/41/28, not the design's stale "40/35/25" copy | `schemas/profile.py`, iOS `Theme`/Settings | unit test on the derived-% computation; Settings row displays derived % |
| AC9 | Seeded "Push Day" program (5 exercises: name·scheme·weight·muscle tag); `GET /train` returns program + today's set events + score | `migrations/`, `routes/train.py` | integration test: program + exercises seeded; day read shape |
| AC10 | `POST /train/sets` marks a set done (idempotent on `UNIQUE(uid,exercise,set,day)`); `DELETE` unmarks; **score = 512 + today's done count**; week strip = session-days-this-week | `routes/train.py`, `repositories/train.py` | integration: toggle idempotency (replay→200), score math, week strip; concurrency replay test |
| AC11 | `GET /body` returns 13 muscle groups × 6-level scale (seeded base levels) + **trained-today flags derived from today's set events** (Push Day → Chest/Shoulders/Triceps) | `routes/body.py`, iOS `Figures/` | integration: levels + trained-today derivation; figure geometry from `figure-paths.md` verbatim |
| AC12 | `POST /weight` stores canonical **kg**; `GET /weight` returns 30-day window + latest + weekly delta; future timestamp rejected 422 | `routes/weight.py`, `repositories/weight.py` | integration: kg storage, 30-day window bound, future-ts 422 |
| AC13 | `PATCH /profile` persists **allowlisted** settings (palette, units, gender, planet_index, kcal_budget, macro grams); reads reflect changes; unknown/other-owner fields ignored | `routes/profile.py`, `schemas/profile.py` | integration: round-trip each field; mass-assignment attempt ignored; iOS Settings persistence |
| AC14 | **Every collection query bounded** — day-/window-scoped AND hard LIMIT (entries/day ≤200, weights 30-day); no unbounded SELECT | `src/orbit/repositories/` | test asserts LIMIT/scope on each list query; over-cap read truncates |
| AC15 | Client-provided timestamps: **backdating allowed, future (> server now + skew) rejected 422** | `schemas/common.py` | unit + integration: backdated accepted, future rejected |
| AC16 | **Input validation (all input sources):** every body a Pydantic `extra="forbid"` model + typed bounds; query params typed/bounded; malformed/oversized/wrong-type → 422; **param-less endpoints reject unknown params (incl. `GET /weight`, `GET /profile`, `GET /catalog/quick-foods`; N/A: `/health`)**; each DB constraint violation → mapped 4xx, never 500 | `src/orbit/schemas/`, `edge/errors.py` | per-input 422 tests; each migration constraint → 4xx test; undeclared-param → 422 (incl. `GET /weight?day_key=`); `/health` N/A |
| AC17 | **Rate limiting:** Tier-1 IP-keyed edge throttle (`/health` exempt, client IP via trusted XFF from ALB); Tier-2 **uid-keyed** on write endpoints; `429 + Retry-After` | `src/orbit/edge/ratelimit.py` | two-principals-one-IP test (A 429s, B same IP still 200; one principal across two IPs shares a bucket); `/health` never throttled |
| AC18 | Security headers on every response (incl. errors: HSTS, nosniff, CSP, frame-deny, referrer); CORS allowlist (never `*`+credentials); error-envelope leaks **no** stack/SQL/type/path and fails closed | `src/orbit/edge/` | header-presence test; safe-error test (forced internal error → generic envelope, no leak, no partial write) |
| AC19 | Structured logs (structlog) for all five audit categories (auth/access-control/CRUD/admin/user-security), 5W+H, **hashed uid, no PII/secret**; validation failures at `warn` | `src/orbit/logging/` | test asserts event fields + absence of PII/secret; audit event on delete + sign-out |
| AC20 | **data_protection:** personal fitness data + `owner_uid` stored → **SSE at rest** (RDS `storage_encrypted=true` + KMS) + TLS in transit; persisted form not plaintext for any sensitive field. `data_protection_waiver: owner_uid — opaque Firebase account key, must be indexable for ownership, no PII stored locally (email/name in Firebase); no credential/sensitive-PII field stored → no field-level KMS/KDF needed` | `infra/modules/data`, `src/orbit/crypto/` | Checkov asserts RDS SSE + TLS in `infra/`; security `data_surface` reconciliation |
| AC21 | **data_lifecycle:** retention declared per field (personal = life of account; none survive deletion); deletion = **hard cascade** on account deletion; backups honesty (≤7d age-out); export deferred | `src/orbit/lifecycle/erase.py`, plan §Data-lifecycle | erasure-cascade test (§AC5); retention declared in plan; backup window stated |
| AC22 | **Runtime secrets** (DB URL, Firebase Admin JSON) fetched from Secrets Manager/SSM behind one facade; none in source / committed `.env` / Docker `ENV` / `.tfvars`; not logged; DB engine re-resolves creds (rotation-aware) | `src/orbit/config/secrets.py`, `infra/modules/secrets` | Gitleaks/Semgrep clean; security 6a/6e; facade-is-only-SDK-caller test |
| AC23 | **Performance:** p95 **< 300 ms @ ~10 concurrent** on `GET /fuel`, `GET /train`, and `POST /fuel/entries` | API read/write paths | k6 `constant-arrival-rate` vs out-of-process uvicorn + testcontainers Postgres; true nearest-rank p95; scenario disclosed |
| AC24 | **Migration round-trip (create-migration kind):** up→down→up restores **schema + all CHECK/FK/UNIQUE/NOT NULL** (no row-survival guarantee — down drops tables); seeds applied via migration | `migrations/versions/` | round-trip test on seeded prod-shaped data; re-verify every constraint after up |
| AC25 | Backend test coverage **≥ 80%** (surfaced; gate = criteria completeness + perf pairing) | `tests/`, `pipeline-ci.yml` | `pytest --cov=src --cov-fail-under=80`; combined coverage in `test-results.json` |
| AC26 | **DAST readiness:** served OpenAPI at `/openapi.json` matching routes (DAST-1); seeded non-prod DAST test user (DAST-2); auth-context recorded — Bearer Firebase ID token (DAST-3) | `src/orbit/main.py`, `scripts/seed_dast_user.py`, plan | schema reachable + matches routes; seed script present; auth context in plan |
| AC27 | **Smoke flow against the real API:** register → sign in → quick-add food → toggle sets (score ticks, Body glows) → log weight → switch palette/units (persists) → delete account (cascades) — all work | iOS + backend end-to-end | XCUITest against the real API + manual walkthrough (reduced-assurance) |
| AC28 | **Theme:** 4 palette presets recolor **everything** (UI, charts, figures, 3D, stars); **no hardcoded hues**; `Theme` struct computed from the 3-color palette; blend/6-stop-scale math correct | iOS `DesignSystem/Theme.swift` | Swift Testing on blend/tint/level-scale math; snapshot per preset (advisory); grep no inline hex outside Theme |
| AC29 | **High-fidelity replication** of the 4 screens + Settings + 25 components per the approved design-spec (`SCREEN-1…5`, `CMP-1…25`); muscle figures **verbatim** from `figure-paths.md`; each departure a flagged native adaptation (NM-1…9) | iOS `Screens/`, `Components/`, `Figures/` | swift-snapshot-testing vs reference (advisory) + human diff-review; design→AC trace in plan |
| AC30 | **Accessibility:** Reduce Motion freezes starfield/ships/float/scroll-3D; VoiceOver exposes value+target on rings/bars, set circles as "Set n, done/not done", muscle rows as "{group},{level},trained today"; hit targets ≥ 44 pt | iOS `Screens/`, `Components/`, `Space/` | XCUITest with Reduce Motion + VoiceOver traits; hit-target audit |
| AC31 | **Visual system staged LAST:** starfield (deterministic seeds, shooting stars, toggleable ships) + SceneKit heroes (Home planet+rings, Fuel macro moons, Train asteroid) + scroll-driven cameras, all Reduce-Motion-gated | iOS `Space/` | snapshot/manual review (advisory); built in the final two tasks (T17–T18) |
| AC32 | **App Store readiness (mechanically gated subset):** `PrivacyInfo.xcprivacy` present, declares collected data types + `UserDefaults` Required-Reason (SC-1/SC-9); no capability API without its `NS…UsageDescription` (SC-2 — stubs inert); ATS not disabled (SC-3); `ITSAppUsesNonExemptEncryption` set (SC-8); release strips debug logging/test hooks. Sign in with Apple / IAP: **N/A** (email-password only; no monetization). Full nutrition-label + signing/screenshots: advisory-deferred (docs/roadmap.md: App Store submission pass) | iOS `Resources/PrivacyInfo.xcprivacy`, `Info.plist` | `scripts/ci/store-compliance.sh` (critical=0); manifest audit vs data map |
| AC33 | **ASVS 5.0 L1/L2 reconciliation** across triggered chapters (V1,V2,V4,V6,V7,V8,V9,V11,V12,V13,V14,V15,V16) met or waived; recorded waivers (6.3.3 MFA; 6.2.x Firebase-delegated) honored | backend + `infra/` | **delegated: security** (`security-status.json` asvs block; not a test-suite assertion) |
| AC34 | **Session lifecycle (ASVS-DET T2-4 / V7):** `POST /me/signout` calls `revoke_refresh_tokens(uid)` → the prior ID token is **rejected 401 on the next request** (via `require_auth`'s `check_revoked=True`), not merely discarded client-side; a fresh sign-in mints a new token (rotation on auth, 7.2.4). iOS Sign out calls the endpoint before clearing the Keychain token | `src/orbit/auth/`, `routes/me.py`, iOS `AuthService`/`SettingsSheet` | integration: sign-out then reuse old token → 401; rotation-on-sign-in test; XCUITest sign-out flow |
