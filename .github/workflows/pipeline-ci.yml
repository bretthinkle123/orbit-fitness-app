# pipeline-ci.yml — per-project CI merge gate (PR L, Layer 2).
# Written to .github/workflows/pipeline-ci.yml by bootstrap-project.sh, which normally fills
# the test/build steps from its --test/--build flags (same channel as smoke.env); on this
# project bootstrap left them as placeholders, so implementation (T1) fills all four:
# install, build, test, and the coverage floor (mirrors test-conventions — the SAME figure
# the local gate rides, so CI and the local gate can't disagree about the bar).
#
# WHY: the pipeline's gates run pre-merge on the AUTHOR'S machine. CI re-RUNS every
# objective check on the merge commit — it never re-reads .pipeline/ artifacts (those are
# the author's claims; re-execution is the only honest verification). Agent-judgment gates
# (criteria mapping, reconciliations) remain pre-merge guarantees; branch protection +
# required human review are their CI-side counterpart. See plan/ci-merge-gate-plan.md.
#
# The re-run scripts live in scripts/ci/ — copied INTO this repo at bootstrap (committed,
# human-reviewed, pinned at the bootstrap-time version) so CI never fetches them remotely.
# SCAN_BASE rescopes their diff to merge-base…HEAD (git diff HEAD is empty on a merge commit).
#
# Waivers: CI honors ONLY committed, tool-native ignore files (osv-scanner.toml,
# .trivyignore, .semgrepignore) — visible in the diff, human-reviewed. The local-only
# .pipeline/waivers.json never reaches CI by design. asvs-sast + source markers are
# fix-not-waive: no ignore path is offered.
#
# Supply-chain conventions: first-party actions pinned by commit SHA; scanners run from
# their official images, version-tagged — pin image DIGESTS on first CI run (same posture
# as exact-pin deps). Least-privilege permissions; per-ref concurrency; hard timeouts.
name: pipeline-ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: pipeline-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  # PRs: scan merge-base..HEAD. Pushes to main: scan the pushed range. RAW because on a
  # first/force push `github.event.before` is the all-zeros SHA — each diff-scoped job
  # resolves it via the "Resolve scan base" step (fallback HEAD^) instead of failing
  # closed on a non-reason.
  SCAN_BASE_RAW: ${{ github.event.pull_request.base.sha || github.event.before }}

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      # Exact-pin the Firebase CLI (same posture as our exact-pinned Python deps), matched to
      # the version the authoring host runs so CI and local agree on emulator behaviour.
      # Single source of truth for BOTH the install below and the emulator cache key — bumping
      # it here invalidates the cache automatically instead of serving a stale JAR.
      FIREBASE_TOOLS_VERSION: 15.18.0
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Placeholder guard (ledger M4-1 — a green job with live placeholders is vacuous)
        # M4 shipped with the coverage-floor placeholder unfilled and no coverage gate
        # anywhere, and this job stayed green. Fail loudly instead. Patterns are composed
        # so this step's own source lines can never match themselves.
        run: |
          F=.github/workflows/pipeline-ci.yml
          PAT="<(INSTALL"; PAT="${PAT}_CMD|BUILD_CMD|TEST_CMD|COVERAGE_FLOOR)>"
          if grep -nE "$PAT" "$F"; then
            echo "::error::Unfilled placeholders in enabled CI jobs. bootstrap fills TEST/BUILD; implementation fills INSTALL; testing fills COVERAGE_FLOOR from CLAUDE.md's done-bar."
            exit 1
          fi
          # Opt-in jobs (mutation, codeql) may keep placeholders ONLY while `if: false`
          # gates them — one gate per opt-in job (M4P-15: codeql shipped enabled with a
          # literal placeholder matrix and was red on every run).
          M1="<MUTATION"; C1="<CODEQL"   # composed so these lines never match themselves
          NGATES=$(grep -cE '^\s*if: false' "$F")
          NOPT=0
          grep -qE "${M1}_CMD>|${M1}_SCOPE>" "$F" && NOPT=$((NOPT+1))       # mutation tokens present
          grep -qE "${C1}_LANGUAGE>|${C1}_BUILD_MODE>" "$F" && NOPT=$((NOPT+1))  # codeql tokens present
          if [ "$NGATES" -lt "$NOPT" ]; then
            echo "::error::an opt-in job (mutation/codeql) appears enabled while its placeholders are unfilled — gate it with 'if: false' or fill the tokens."
            exit 1
          fi
      - name: Install dependencies (from lockfile)
        # CONTRACT (M4P-9): the install must make the project package importable from a
        # SUBPROCESS spawned anywhere in the repo. `poetry install` installs the root
        # project (src/orbit) into the venv, so `import orbit` resolves regardless of the
        # subprocess's working directory — the contract holds via the venv, not via cwd.
        #
        # Poetry manages its OWN venv here — do NOT set `virtualenvs.create false`.
        # That config made poetry install into the runner's SYSTEM interpreter, where pip
        # then had to UNINSTALL ubuntu-latest's pre-installed root-owned botocore (a
        # transitive dep of our pinned boto3) and died with
        # `PermissionError: [Errno 13] Permission denied: 'configloader.py'` →
        # "Cannot install botocore." The job is not root and must never fight the runner
        # image's system site-packages. Every later step that needs project deps therefore
        # runs through `poetry run` (see Build / Test below) — keep that consistent.
        run: |
          pip install --no-cache-dir poetry==2.4.1
          poetry install --no-interaction --no-ansi
      - name: Build
        # No compiled-artifact build step for a direct-process Python service; "build"
        # here is a byte-compile pass so a syntax error fails fast, before the test step.
        run: poetry run python -m compileall -q src
      - name: Install Firebase CLI (the test suite boots the Auth emulator)
        # tests/conftest.py's session-scoped `firebase_emulator` fixture shells out to
        # `subprocess.Popen(["firebase", "emulators:start", "--only", "auth", ...])` to mint REAL
        # tokens against a live emulator (no mocked auth guard). ubuntu-latest does NOT ship
        # firebase-tools, so without this step the Test step dies immediately with
        # `FileNotFoundError: [Errno 2] No such file or directory: 'firebase'`.
        #
        # The emulator's OTHER prerequisites need no step — they come from the runner image:
        # Java (the Auth emulator is a JAR) and Docker (testcontainers Postgres/Redis) are both
        # pre-installed on ubuntu-latest, as are node/npm. `alembic` — the only other binary the
        # suite shells out to (tests/conftest.py, tests/integration/test_migrations.py) — is a
        # project dependency, so `poetry run pytest` puts the venv's bin on PATH and the
        # subprocess resolves it from there; it needs no global install.
        run: npm install -g "firebase-tools@${FIREBASE_TOOLS_VERSION}"
      - name: Cache Firebase emulator JARs
        # `emulators:start` downloads the Auth emulator JAR into ~/.cache/firebase/emulators on
        # first use; cache it so it isn't re-fetched on every run. Keyed on the pinned CLI
        # version, so a bump invalidates the entry rather than restoring a stale JAR. Purely an
        # optimisation — a miss just re-downloads, it never fails the job.
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ~/.cache/firebase/emulators
          key: ${{ runner.os }}-firebase-emulators-${{ env.FIREBASE_TOOLS_VERSION }}
      - name: Test with coverage (floor = the local gate's figure)
        # Coverage floor 80 mirrors CLAUDE.md's done-bar / test-conventions — the SAME
        # figure the local deploy gate rides, so CI and the local gate can't disagree.
        # Perf-marked tests are excluded here (timing is owned by local runs + the
        # load-campaign workflow against staging, per M4P-10 — this runner's timing is
        # not representative of the perf budget).
        run: poetry run pytest --cov=src --cov-report=term-missing --cov-fail-under=80 -m "not perf"
        # Branch coverage surfaces in the job summary; it is not itself a gate (mirrors
        # the local posture; see test-conventions).

  # mutation — U-22: run mutation testing HERE, on the Linux runner, not on the author's
  # host. mutmut needs a POSIX environment (WSL on Windows), so the local pipeline honestly
  # skips it and writes quality_ok:false (WS3-1) — the *measurement* just never happened on
  # win32, two M3-series runs in a row. This job runs it where it works. ADVISORY: it reports
  # the score in the job summary and NEVER fails the merge gate (mutation quality is
  # settled-advisory; the local WS3-1 scope-honesty check stays the only deploy-side teeth).
  # OPT-IN: fill <MUTATION_CMD> + <MUTATION_SCOPE> (the logic-dense changed modules, matching
  # test-conventions) to enable; leave the placeholders and delete this job if not using it.
  mutation:
    if: false   # flip to `true` (or a repo/branch condition) once <MUTATION_CMD> is filled
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Install dependencies (from lockfile)
        # Same contract as build-and-test: poetry manages its own venv (no
        # `virtualenvs.create false` — it collides with the runner's root-owned system
        # packages), so the mutation step below runs through `poetry run`.
        run: |
          pip install --no-cache-dir poetry==2.4.1
          poetry install --no-interaction --no-ansi
      - name: Mutation testing (advisory — score to the summary, never fails the gate)
        run: |
          set +e
          poetry run <MUTATION_CMD>   # e.g. poetry run mutmut run --paths-to-mutate <MUTATION_SCOPE>
          echo "Mutation run complete (advisory; see output). This job never fails the merge gate." >> "$GITHUB_STEP_SUMMARY"
          exit 0

  sast:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Semgrep (full tree; criticals fail, warnings annotate)
        run: |
          docker run --rm -v "$PWD:/src" -w /src semgrep/semgrep:latest \
            semgrep scan --config auto --severity ERROR --error .
          # Use the same rule packs the project's semgrep-ruleset-guide names; honors a
          # committed .semgrepignore. Pin the image digest on first run.

  # codeql — deep SAST (roadmap row CQ): semantic taint/dataflow analysis beyond Semgrep's
  # pattern matching; findings surface as code-scanning ALERTS on the PR/merge commit (the
  # job fails only on analysis error, not on findings — added assurance, not a delivery
  # step). CI-only BY DESIGN: it builds a database and is slow, so it stays out of the local
  # security⇄debug⇄test loop (plan/ci-merge-gate-plan.md Layer 4). Free on public repos;
  # private repos need GitHub Advanced Security — if neither applies, delete this job (the
  # Semgrep/ASVS layers above still run). One include entry PER LANGUAGE, each paired with
  # its build mode (duplicate the entry for a second language):
  #   build-mode none — javascript-typescript, python, ruby, c-cpp, csharp, java-kotlin, rust
  #   build-mode autobuild — go
  #   swift — needs a macOS runner + autobuild/manual; deferred with the iOS roadmap row
  # NOTE: if the repo has CodeQL "default setup" enabled in Settings, disable it first —
  # it rejects uploads from advanced (workflow) configurations like this one.
  codeql:
    # OPT-IN like mutation (M4P-15): as bootstrapped the matrix carries literal
    # placeholders, so an enabled job fails on every run — vacuous red that trains people
    # to ignore the gate. Flip to `true` only after filling <CODEQL_LANGUAGE>/<CODEQL_BUILD_MODE>.
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      security-events: write   # upload results to code scanning (per-job elevation)
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: <CODEQL_LANGUAGE>       # e.g. python
            build-mode: <CODEQL_BUILD_MODE>   # none | autobuild — see the table above
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - uses: github/codeql-action/init@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-extended
      - uses: github/codeql-action/analyze@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
        with:
          category: "/language:${{ matrix.language }}"

  deps:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with: { fetch-depth: 0 }   # SCAN_BASE needs history
      - name: Resolve scan base (all-zeros on a first/force push → fall back to HEAD^)
        id: base
        run: |
          B="$SCAN_BASE_RAW"
          case "$B" in ""|0000000000000000000000000000000000000000) B="" ;; esac
          [ -n "$B" ] && git rev-parse --verify --quiet "$B^{commit}" >/dev/null || B="$(git rev-parse --verify --quiet HEAD^ || true)"
          echo "sha=$B" >> "$GITHUB_OUTPUT"
          [ -n "$B" ] || echo "::warning::no resolvable scan base (single-commit repo) — diff-scoped checks skipped this run"
      - name: OSV scan (CVSS >= 7.0 fails — the B6 floor; honors committed osv-scanner.toml)
        # Image pinned by DIGEST (M4P-14): the unpinned :latest broke on an upstream flag
        # removal (--lockfile-scan-mode, exit 127) — the exact drift digest-pinning exists
        # to prevent. Bump the digest deliberately, in a reviewed diff.
        # M4pp-A9 (scanner liveness): a scanner MALFUNCTION (exit 127-class) must read as
        # "broken scan step", never blend into "dependency findings" — the 127 above was
        # misread for two features while it masked a real CVE. Findings (exit 1 + a
        # parseable result) and malfunction (anything else) now fail with distinct errors.
        run: |
          set +e
          docker run --rm -v "$PWD:/src" -w /src \
            ghcr.io/google/osv-scanner@sha256:5116601dedc01c1c580eb92371883ec052fc4c13c3fbc109d621a63ac416d475 \
            scan -r --format json --output /src/osv-result.json /src
          rc=$?
          set -e
          if [ "$rc" -eq 0 ]; then echo "OSV clean"; exit 0; fi
          if [ "$rc" -eq 1 ] && jq -e . osv-result.json >/dev/null 2>&1; then
            echo "::error::OSV found vulnerabilities (unwaived by osv-scanner.toml):"
            jq -r '[.results[]?.packages[]?.vulnerabilities[]?.id] | unique | .[]' osv-result.json
            exit 1
          fi
          echo "::error::OSV scanner MALFUNCTION (exit $rc, no parseable result) — a broken scan step, NOT a findings failure; fix the step, do not read this as dependency state (M4pp-A9)"
          exit "$rc"
      - name: Lockfile integrity vs merge base (exit 2 blocks; exit 1 = warn, annotated not failed)
        if: steps.base.outputs.sha != ''
        run: |
          set +e   # steps run bash -e; capture the tri-state exit code ourselves
          SCAN_BASE="${{ steps.base.outputs.sha }}" bash scripts/ci/lockfile-check.sh
          rc=$?
          set -e
          if [ "$rc" -eq 1 ]; then echo "::warning::lockfile-check reported warnings (see log)"; exit 0; fi
          exit "$rc"

  secrets:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with: { fetch-depth: 0 }   # full history — CI can afford it (local hook is diff-scoped)
      - name: Resolve scan base
        id: base
        run: |
          B="$SCAN_BASE_RAW"
          case "$B" in ""|0000000000000000000000000000000000000000) B="" ;; esac
          [ -n "$B" ] && git rev-parse --verify --quiet "$B^{commit}" >/dev/null || B="$(git rev-parse --verify --quiet HEAD^ || true)"
          echo "sha=$B" >> "$GITHUB_OUTPUT"
      - name: Gitleaks over branch history (full tree when no base resolves)
        run: |
          if [ -n "${{ steps.base.outputs.sha }}" ]; then
            docker run --rm -v "$PWD:/src" -w /src zricethezav/gitleaks:latest \
              detect --source /src --log-opts="${{ steps.base.outputs.sha }}..HEAD" --redact -v
          else
            docker run --rm -v "$PWD:/src" -w /src zricethezav/gitleaks:latest \
              detect --source /src --redact -v
          fi

  containers-iac:
    # M4pp-A9 (scanner liveness): Trivy and Checkov are INDEPENDENT steps — for two
    # features the Trivy step failed first and short-circuited Checkov, which had
    # therefore NEVER run; its true surface (48 failed checks) surfaced only when Trivy
    # was fixed. Each scanner runs regardless of the other (continue-on-error +
    # if: always()), each must emit a parseable result artifact (absence ≠ clean; a
    # disclosed no-infra skip writes a sentinel), and a final liveness step fails the
    # job on any missing artifact or failed scanner — so "scanner never ran" and
    # "scanner ran, found nothing" are mechanically distinct states.
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Trivy fs (vuln + secret + misconfig; honors committed .trivyignore)
        id: trivy
        continue-on-error: true   # a Trivy red must not stop Checkov; the liveness step re-fails the job
        run: |
          docker run --rm -v "$PWD:/src" -w /src aquasec/trivy:latest \
            fs --scanners vuln,secret,misconfig --exit-code 1 --severity HIGH,CRITICAL \
            --format json --output /src/trivy-result.json /src
      - name: Checkov (only when infra/ exists)
        id: checkov
        if: always()               # runs even when Trivy failed (the M4pp short-circuit)
        continue-on-error: true
        run: |
          if [ -d infra ]; then
            docker run --rm -v "$PWD:/src" -w /src bridgecrew/checkov:latest \
              -d /src/infra -o cli -o json --output-file-path console,/src/checkov-result.json
          else
            echo '{"skipped":"no infra/ in this repo"}' > checkov-result.json
            echo "no infra/ — skipped (sentinel artifact written)"
          fi
      - name: Scanner liveness + verdict (M4pp-A9)
        if: always()
        run: |
          fail=0
          if ! jq -e . trivy-result.json >/dev/null 2>&1; then
            echo "::error::Trivy produced no parseable result artifact — a scanner that never ran is not a clean scan"; fail=1
          fi
          if ! jq -e . checkov-result.json >/dev/null 2>&1; then
            echo "::error::Checkov produced no parseable result artifact (and no disclosed skip sentinel)"; fail=1
          fi
          if [ "${{ steps.trivy.outcome }}" = "failure" ]; then
            echo "::error::Trivy failed (findings or crash — see its step log)"; fail=1
          fi
          if [ "${{ steps.checkov.outcome }}" = "failure" ]; then
            echo "::error::Checkov failed checks (see its step log)"; fail=1
          fi
          exit $fail

  asvs-markers:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with: { fetch-depth: 0 }
      - name: Resolve scan base
        id: base
        run: |
          B="$SCAN_BASE_RAW"
          case "$B" in ""|0000000000000000000000000000000000000000) B="" ;; esac
          [ -n "$B" ] && git rev-parse --verify --quiet "$B^{commit}" >/dev/null || B="$(git rev-parse --verify --quiet HEAD^ || true)"
          echo "sha=$B" >> "$GITHUB_OUTPUT"
      - name: ASVS Tier-1 patterns vs merge base (fix-not-waive; exit 2 fails)
        if: steps.base.outputs.sha != ''
        run: SCAN_BASE="${{ steps.base.outputs.sha }}" bash scripts/ci/asvs-sast.sh
      - name: Experimental-revert / must-not-ship markers vs merge base (fix-not-waive; exit 2 fails)
        if: steps.base.outputs.sha != ''
        run: SCAN_BASE="${{ steps.base.outputs.sha }}" bash scripts/ci/guard-source-markers.sh

  store-compliance:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: App-store Tier-1 checks (repo-state scoped; no-ops on non-mobile projects)
        run: |
          mkdir -p .pipeline && echo '{}' > .pipeline/state.json   # satisfy the ambient guard
          bash scripts/ci/store-compliance.sh
          crit=$(jq -r '.critical // 0' .pipeline/store-compliance.json 2>/dev/null || echo 0)
          echo "store-compliance critical=$crit"
          [ "$crit" -eq 0 ] || exit 1   # mirror the local deploy-gate floor

  # dast-baseline — filled per plan/dast-plan.md when the app has a bootable HTTP surface:
  # boot the app (service container or <START_CMD>), run the ZAP passive baseline against
  # it, then scripts/ci/dast-review.sh vs the committed dast-budget.json (blocking on High
  # here, per the DAST plan's promotion rule). Inert until wired.
  # dast-baseline:

  # deploy-verify — the reborn post-deploy-check: probes DEPLOY_HEALTH_URL after PR N's
  # deploy workflow reports success. Inert (skipped) until PR N provides the URL.
  deploy-verify:
    if: ${{ vars.DEPLOY_HEALTH_URL != '' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Post-deploy health probe
        run: curl -sf -m 10 "${{ vars.DEPLOY_HEALTH_URL }}" >/dev/null
