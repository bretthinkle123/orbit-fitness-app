"""Operational scripts package (not part of the deployed application runtime)
— makes `scripts/seed_dast_user.py` importable from tests as `scripts.
seed_dast_user`, mirroring the `tests/` package's own import mechanics.
"""
